# SVIROXK — статус компилятора

## Этап 1 — завершён

Все базовые возможности языка и LLVM-компилятор работают (проверено в Termux).

## Сделано

| Область | Статус |
|---------|--------|
| Лексер + номера строк в ошибках | ✅ |
| Операторы `+ - * /` с обычным приоритетом | ✅ |
| Переменные, массивы, условия, циклы, функции | ✅ |
| Указатели, файлы, импорт, ввод/вывод | ✅ |
| Структуры + `p.field` | ✅ |
| Инициализация: `svi tip Player lor p = { health = 80; };` | ✅ |
| `fucn dor имя(...)` — возврат `dor`/`cos`/и др. | ✅ |
| Типы параметров в `fucn` | ✅ |
| `rep()`, `test.sh` | ✅ |

## Этап 2 — самохостинг (следующий большой шаг)

- [ ] Переписать компилятор на SVIROXK
- [ ] Собрать SVIROXK-компилятор самим собой

## Проверка в Termux

```bash
git pull
chmod +x build.sh run.sh test.sh
./test.sh          # все примеры
./run.sh hello.svx # только привет мир
```

`examples/struct.svx` выводит `80` — это `hero.health`, не ошибка.
